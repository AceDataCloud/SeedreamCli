apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: seedream-cli
  name: seedream-cli
  namespace: acedatacloud
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: seedream-cli
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: seedream-cli
    spec:
      containers:
        - name: seedream-cli
          image: ghcr.io/acedatacloud/seedream-cli:${TAG}
          imagePullPolicy: IfNotPresent
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
      dnsPolicy: ClusterFirst
      restartPolicy: Always
